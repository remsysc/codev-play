info:
  name: login player 2
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/auth/login"
  body:
    type: json
    data: |-
      {
             "username": "player2",
             "password": "Password123"
            }
  auth:
    type: bearer
    token: "{{token_player2}}"

runtime:
  scripts:
    - type: after-response
      code: |-
        if (res.getStatus() === 201) {
                  const body = res.getBody();
                 if (body.data && body.data.token) {
                     bru.setEnvVar("token_player2", body.data.token);
                     console.log("✅ Token saved to environment");
                 }
             }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
