info:
  name: join game player 2
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/games/rps/{{gameId}}/join"
  auth:
    type: bearer
    token: "{{token_player2}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
