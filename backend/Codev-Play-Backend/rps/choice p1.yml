info:
  name: choice p1
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/games/rps/{{gameId}}/choice"
  body:
    type: json
    data: |-
      {
        "choice" : "R"
      }
  auth:
    type: bearer
    token: "{{token_player1}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
