info:
  name: get game state
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/games/rps/{{gameId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
